import 'package:flutter/material.dart';

class AppColors {
  // --- Dark Theme Colors ---
  static const Color darkPrimary = Color.fromARGB(255, 0, 122, 255);
  static const Color darkAccent = Color(0xFF007AFF);
  static const Color darkBackground = Color(0xFF181A20);

  // --- Light Theme Colors ---
  static const Color lightPrimary = Colors.deepPurple; // Example
  static const Color lightBackground = Colors.white;
}
